steps:
- name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
  args:
  - gcloud
  - functions
  - deploy
  - file-processing
  - --region=eu-west1
  - --source=.
  - --trigger-bucket=immobiliare-daily-data
  - --runtime=python311
  - --gen2
